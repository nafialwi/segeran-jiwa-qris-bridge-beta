# WP10 GitHub Patch v0.3.0
Copy these files into the existing `nafialwi/segeran-jiwa-qris-bridge-beta` repository preserving paths. Do not upload the patch ZIP itself as a nested directory. Existing GitHub signing secrets and stable keystore identity remain unchanged.
